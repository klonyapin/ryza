# 役員室・会議形式化(ルータ+決定論ガード)独立役員審査意見書(2026-08-03)

- 審査者: 独立役員(personas/independent-officer/charter.md v1.0)。起草者の選好は不知
- 対象: boardroom.py(会議化・route_speakers・conduct_meeting・決定論ガード)、dashboard/app.py、provenance/runs.py(update_params)、05-governance.md §5。**05 は L2 のため本意見書が L2 独立役員レビューを兼ねる**
- 判定: **条件付き承認**(C-1〜C-3 の反映を前提。C-4・C-5 は同時または期限付き是正)

## 条件

- **C-1(重大)**: ガードは代表の最新1発言のみを見るため、多ターン分割と言い換えで素通りする。実測で「明日から本番でいこう」「デモはもう十分だ」「あと100万ほど」「go live with real capital」が全て未検出、一方 "tips" が `ips` 部分一致で誤検出。「LLM+決定論の二重担保」は現状成立せず実質一重。判定範囲を前回 IO 発言以降の代表発言に拡張し、口語・英語・`N万/N株/N倍` を追加。加えて **`mark_resolution` 側で「独立役員の発言なき決議」を検出する**決定論チェックを置く(決議は文言に依存しないため言い換えに強い)
- **C-2(重大)**: 発言を無サニタイズで連結するため、役員の出力に `代表: …` を含めるだけで後続役員・ルータ・議事録本文に代表の発言として混入する。防御はプロンプト1行のみ。話者ラベル行の無害化とフェンス化を要する(証憑の完全性=不変原則3)
- **C-3(重)**: stances 要約の入力が会議全文になり、記憶分離が「入力が構造的に role-pure」から「プロンプト依存」へ後退した。当該 role の発言のみに決定論フィルタすること。あわせて stances に出所種別を持たせ、盲検レビュー着任時に会議由来を除外できるようにする(代表の選好が stances 経由で盲検経路に透過する)
- **C-4(重)**: `minutes.run_id` は保存 run を指し routing を記録した会議 run と別。update_params は成功時のみ・選定元(router/guard/facilitator)を区別しない。§5 が謳う §6-5 の事後検証は現状成立しない
- **C-5(中)**: §2 は minutes を永続記憶と定義しており、§5 の「§6-2 の範囲外」は文書内で矛盾する。緩和は §6-2 本文の改訂として行うこと

## その他(非ブロック)

C-6 進行役フォールバックが執行側(CIO)固定で境界域の既定の声が執行に傾く / C-7 `update_params` が入力証跡を可変化(専用キー+`params || jsonb` を推奨)/ C-8 「費用の上限」は呼び出し回数の上限に過ぎずトークンは O(n²) / C-9 ガードの置換規則が監査を押し出す・`ips` 部分一致の誤検出 / C-10 上限4の全経路成立は確認済(呼出側上書き可のためハードシーリング推奨)

## 評価すべき点

選択制チャットでは代表が独立役員を選ばない限り批判は発生しなかった。本変更は批判の露出を純増させており、ガードの不完全性は後退を意味しない。

## 設計リード裁定(2026-08-03 追記)

- 本 PR 内で是正: C-1(判定範囲拡張+語彙追加+語境界判定+**mark_resolution の決定論チェック** —
  独立役員の発言が無い議事録の決議マークは警告と確認を要求)、C-2(話者ラベル行の無害化+フェンス化)、
  C-3 の決定論フィルタ、C-4(選定元ラベル router/guard/facilitator を議事録本文のメタ節に記録)、
  C-5(§6-2 本文の改訂)、C-6(フォールバックは LLM なしの定型文に変更)、
  C-7(params["runtime"]+`params || jsonb` マージ)、C-8(§5 文言の正確化+トランスクリプト窓 = 直近30発言)、
  C-9(置換は critic・audit 以外を優先)、C-10(min(max_speeches, MAX_SPEECHES_PER_TURN))。
- リマインダー登録して後続: stances の出所種別カラム(protected migration。盲検着任時の会議由来除外とセット。
  期限 2026-08-17)。
- 是正後の差分は独立役員の再確認(限定スコープ)を経てからマージする。
