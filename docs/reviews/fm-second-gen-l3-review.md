# FM 第二陣(Stan・Peter)役職資産 L3 盲検レビュー(独立役員・2026-08-04)

- 対象: personas/fm-stan/(charter+system)・personas/fm-peter/(同)・docs/tasks/T-019-fm-second-gen.md(origin/main..HEAD、盲検 — 起草者・代表の選好は不知)
- C-1(要修正・小): Stan system.md「良い例」2つ目「ドル円が 145 円を終値で下回ったら降りる」に時間条件が無く、T-019 の構文検査(日数+系列コード要求)と不整合 — この例を真似た出力は自らの検査で rejected になる。例に時間条件を足すこと
- C-2(要修正・小): Peter の二本条件(成長側+価格側)の機械判定方法が T-019 に未規定(「Stan と同じ位置」のみ)。構文解析より、スキーマを invalidation_growth_md / invalidation_price_md に分割して必須化する方が決定論検査として堅い — 指示書に明記すること
- C-3(要修正・小): 時間条件の N に上限が無く「200 営業日以内」でも検査を通る。切除の読み替え「どれだけ早く降りるか」は invalidation の緩さ経由で実効リスクに残る唯一の LLM 経路であり、N の config 上限(マンデート holding 参照値 days_to_weeks と整合する営業日数)を義務化すること
- C-4(懸念・対応不要): Stan system.md の「高揚は語り口にだけ現れてよい」は帯域外の確信度チャネル(読み手が温度から確信を推定)になりうる。月次再配分は E9 補正済み指標のみ(40 §制約2)で規定上防御済みだが、当該一文は削っても人格は保て、誘発リスクに見合う利得が無い
- 良: 確信度→サイズ命題の切除は全文検査で残存なし(サイズ言及は全て切除・禁止の文脈のみ)。マンデート発効値(レバ 3x/1x・short 可否・集中度・禁じ手)と無矛盾で、short/レバ不使用を「実装の追随」と正しく位置づけ。tests/test_org.py の文言固定・T-019 反対意見書(発注ゼロ FM 増加=E9 検定数増、切除後 Stan の Jim 化)は正面から扱われており質が高い。残る注文: 反対意見1の「着手時点で再判断」の判定主体が reminder に無い・意見2 代替案の棄却理由が一文も無い
- 判定: **要修正(小)** — C-1〜C-3 の3点はいずれも文言・指示書の追記で足り、修正後の再レビューは不要(みなし承認 48h に載せてよい)

## 設計リード裁定と対応(2026-08-04 追記)

**C-1 採用**(対応コミット: 本 PR)。`personas/fm-stan/system.md` の invalidation 例を書き換えた。指摘の「ドル円が 145 円を終値で下回ったら降りる」は**悪い例として明記**(水準だけの条件はいつまで待つのかを決めていない)し、良い例は2つとも「営業日数+系列コード」を含む形に統一した(「20 営業日以内に 10年金利(FRED:DGS10)が 1.2% を上抜けなければ見立ては外れ」「20 営業日以内にドル円(USDJPY)が 145 円を終値で下回らなければ降りる」)。自らの検査で全滅する例示は解消。

**C-2 採用**(対応コミット: 本 PR)。指摘のとおり構文解析は合否判定不能と判断し、T-019 §2-1 を**スキーマ分割**に書き換えた: `invalidation_growth_md` / `invalidation_price_md` の2フィールドを必須とし、決定論検査は「両方が非空」+「各フィールドがスクリーニング計算済みの指標名集合に言及していること」の2点。`trading.fm_theses.invalidation_md` は1本のままとし、**2フィールドは決定論の定型連結で合成して記録する**(マイグレーション不要。合成規則はコード内定数で、LLM に組み立てさせない)。§3 のスキーマ一覧・§4 のテスト(片方だけの拒否・合成文字列のゴールデン)・受け入れ基準4も対応させた。`personas/fm-peter/` の charter と system も同じ2フィールド名で記述を揃えている。
なお**同型の脆さが Stan 側にもあった**ため、裁定を C-2 の射程を超えて適用した: Stan の時間条件も自然文の構文検査をやめ、`invalidation_horizon_days`(整数)+ `invalidation_series`(系列コード配列・**`market.indicators` への実在検査つき**)のフィールド必須に改めた。

**C-3 採用**(対応コミット: 本 PR)。T-019 §1-1 に `config/fm_stan.yaml` の `invalidation.max_horizon_days` を**必須項目**として規定し、上限超過は `rejected`(緩和ではない)とした。上限値そのものは実装時の起案とし、指示書は「上限必須+マンデート `style_reference.holding: days_to_weeks` との整合+**なぜその日数かの根拠コメント**」を義務づける形にしている(§1-3)。`StanConfig.load` が欠落・非正を load 時に落とすこと、およびそのテストも指示に含めた。あわせて `personas/fm-stan/charter.md` の義務にも「N には上限がある/長い N で事実上の無期限保有にしない」を明記した — 指摘のとおり、切除後に残る唯一の実効リスク経路であるため、指示書側だけでなく規範側にも書く。

**C-4 採用**(レビューは「対応不要」としたが設計リード判断で削除。対応コミット: 本 PR)。`personas/fm-stan/system.md` の「その高揚は**語り口にだけ**現れてよく」の一文を削除し、「落ち着いた丁寧な話し方をする。高揚・スリル・勝負勘は判断の根拠にならない。」に置き換えた。**「してよい」という許可の形が帯域外チャネルを開く**という指摘に同意する。人格は charter §禁止(「人格は語り口に現れてよいが、判断の根拠になってはならない」)と system の話し方の記述で保てており、system 側に許可を重ねる利得は無い。

**残る注文①(反対意見1の判定主体)採用**。T-019 §6-1 に「判定するのは**設計リード**、時点は reminder `t019-fm-second-gen-impl` の期日(2026-08-20)、見送る場合は `date` を更新し理由を残す」を明記し、同じ内容を `ops/reminders.yaml` の当該 body にも入れた(指示書だけに書くと、実際に判定を起こすジョブ側からは見えないため両方に書く)。

**残る注文②(意見2 代替案の棄却理由)採用**。T-019 §6-2 に「40・81 の改訂を伴う **L3 の制度変更**であり実装指示書(L4)のスコープ外。採るなら CIO 起案 → 独立役員審査 → みなし承認の別件として起票」と一文を追記した。

判定「要修正(小)・再レビュー不要」に従い、本 PR はみなし承認 48h の経路に載せる。
