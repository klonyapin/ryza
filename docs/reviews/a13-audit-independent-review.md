# A-13(定款監査)実装・配線の独立役員意見書(2026-08-03・要旨)

※監査番号は #48 で A-16 への改番予定(00-system-design の A-13〜A-15 と衝突のため)

## 第1回: 監査本体 d61517d — 条件付き承認

- **懸念1(重大・実証あり)**: 承認判定の2バイパスを攻撃実験で実証 — ①`git merge --no-ff -m "Merge pull request #999..."` の件名偽装で Kill Switch 改変が違反0で素通り ②マージコミット自身の差分(evil merge)は無条件スキップ。「過失検知器であり敵対的検知器ではない」
- 懸念2: env opt-in・所見なし無投稿・例外握りにより沈黙が三義的(稼働/未配線/毎週失敗を区別できない)
- 懸念3: 将来アクションの reminders 未登録(CLAUDE.md 違反)
- **是正(da9137c)**: evil merge を `git diff-tree --cc` で検査・限界3項の常時開示・週次ダイジェストへの実行状態ハートビート・checkout 鮮度検査・reminders 3件登録

## 第2回: VM 配線 deploy-a13.sh — 差し戻し → 実験で一部反証の上、採用

- 懸念1「TOKEN 行の引用符ネストで毎回即死」→ **設計リードが再現実験で反証**(bash の `$()` 内は引用符コンテキストが独立。生成行の動作を実測確認)。議論規約4(争点は証拠で決着)の適用例
- 懸念2〜4(監査コードが rsync デプロイ側から実行される・トークンの argv 露出・障害時の無通知)→ **全て採用**: 監査コードを監査 clone 側から実行(PYTHONPATH)、GIT_ASKPASS+env 経由、OnFailure 通知ユニット

## 第3回: A-13-4(全変更 PR 化検査)c9fe047 — 条件付き承認

- 懸念①: マージ判定が親数のみで A-13-1 の件名検査より緩い → 是正(a1d8762: 件名検査併用・非 PR マージも違反化)
- **懸念②(手続)**: governance.yaml(L1)への統制行追加を「条文変更でない」とする起草者の自己分類は文理解釈上不可 — L1 は代表の明示承認のみ。→ 是正: yaml 変更を撤回し L1 バッチ PR へ分離
- 基準コミット 4c7f6e9(ルール採用時点の main HEAD)は厳密一致で妥当

統合: 監査本体+配線 PR #40、A-13-4 PR #47。
