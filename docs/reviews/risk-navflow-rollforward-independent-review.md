# 独立役員審査: risk/navflow ロールフォワード(重要-5 是正)

- 日付: 2026-08-03 / 対象: `b6819cb` `3d1e780`(navflow.py 新規・risk/daily.py・dashboard/{queries,app,viz}.py)
- 手法: SQL 意味論と `engine.book_returns` を純 Python で再実装し、自作シナリオ8本で手計算突合(DB 不在のためテストは未実行)

**判定: 条件付き承認。** 誤差はシナリオ B で 5,250bp → 250bp に縮小しており方向は正しい。境界(entry_date == 前 snap_date の帰属・系列先頭前フローの数値影響ゼロ・末尾後 pending・単点系列)はいずれも実装と手計算が一致。定義の一本化は実質的で、フロー条件を持つ SQL は navflow の1箇所のみ。起草者の誤 vol_exceeded 主張も再現(r=[0.5,0.0] → EWMA20 年率 754.98%、limit 15%)。

**重要-1(残存・数値)**: ロールフォワードは期末フロー仮定のままで、区間内フローがリターンを (1+F/V₀) 倍に増幅する(手計算: V₀=100万・+50万・市場+5% → 報告 +7.5% / 真値 +5.0%)。EWMA20 年率で F/V₀=1.0・市場+2% のとき 15.0% → 22.7% となり、資本形成期に誤 `vol_exceeded`(新規建てブロック)を出しうる。是正案: `r_t = (nav_t − flow_eop)/(nav_{t−1} + flow_bop) − 1`(検証で真値と厳密一致・現行式に退化する)。
**重要-2(残存・発生確率高)**: 締め実行後に同日付で立った仕訳は救えず、r に ±X% の対を生む(検証: +50%)。是正案: 直近 N 営業日の再締め(`_upsert_nav` は冪等)。
**重要-3(要修正)**: 「ES95 にも波及」は誤り。`es95()` は `book_returns` を参照しない(engine.py:138/216)。
**重要-4(要修正)**: pending 注記を notes 末尾に append し `[:1024]` で切るため(daily.py:277/345)、注記過多日は urgent だけ立って理由が消える。
**重要-8(手続)**: `src/ryza/risk/**` は protected_areas 未登録のまま測定定義を変更。登録依頼を本 PR と同時に出すこと(2026-08-10 のリマインダー待ちにしない)。
**中**: pending 継続中は毎日 urgent(材料性しきい値なし・IBCS 色規約と衝突)/ダッシュボードの二重キャッシュによる整合性スキュー/明細表先頭行の `net_flow` が「設定来累計」に意味変化(無説明)。
**敵対的**: `entry_date` は呼び出し側指定・閉鎖期間ロックなしで帰属点を一意に決めるが、修正前(フロー完全脱落)より操作余地は小さい。要求: 出資・払戻の entry_date と証憑値付け日の突合を posting 側で検証。

## 設計リード裁定(2026-08-03 追記)

- 本 PR 内で是正: **重要-1 の BOP/EOP 分離を採用**(審査提示の式。審査シナリオ B/C/H を回帰テスト化し真値一致を固定。未マージのうちに正しい定義へ)、重要-3(ES95 記述の削除)、重要-4(pending 注記を先頭挿入または独立フィールド)、中-5(pending の urgent は「次の締めを1回跨いだ場合」または NAV 比 0.5% 以上の場合に限定 — 決定論ルール)、中-6(1クエリ・1キャッシュ化)、中-7(明細表の先頭行注記)。
- 同時に別 PR(L1): `src/ryza/risk/**` の protected_areas 登録(重要-8 — 設計リードが起票し #承認 通知・独立審査記録は本意見書を参照)。
- リマインダー登録: `ledger-reclose-recent-days`(重要-2: 締めの直近 N 営業日再締め+nav_snapshots の producer リネージ。期限 2026-08-10)、`flow-entry-date-evidence-check`(敵対的指摘: 出資・払戻 entry_date と証憑値付け日の突合。期限 2026-08-24)。

## 再締め審査記録(2026-08-04 独立役員 — 重要-2 の是正 `3698ebe`)

- 対象: `src/ryza/ledger/closing.py`(**保護領域 ledger_engine**)・`src/ryza/execution/close.py`・`config/execution.yaml`・`jobs/daily.py`。手法: 実 DB でプローブテストを走らせ `book_returns` で観測(対照=再締めなし・全期間窓を併走)
- **判定: 差し戻し。** 重要-2 の主症状(締め後の同日出資)は解消を確認したが、窓の外に落ちた仕訳で**新たな偽リターンを作る**ため、この2点の是正まで保護領域への統合に同意しない
- **再-1(重大)**: 部分再締めは再締めなしより悪い。同一シナリオで対照(N=0)は returns `[0,0,0]`、本実装(N=3・フローは窓の外)は `[+0.5,0,0]`、全期間窓(N=5)は `[0,0,0]`。窓が仕訳日を覆わないと窓境界に **+50% の偽日次リターンが恒久的に立ち**、1 回目の報告後は `reclose` が `[]` を返し完全に無言になる。これは `ewma_vol` 経由で誤 `vol_exceeded` を出しうる(重要-1 と同じ経路)。是正: 固定 N を廃し、**窓の最古日が変化した/水位が進んだ日があれば1日ずつ遡って延長**(上限到達時はアラート)
- **再-2(重大)**: `status` 据え置きの根拠(「遅れて立つのは拠出資本だけ」)がコードと不一致。差分の性質を見ずに `book_totals` 全体を再計算するため、`confirmed` 日に遅延約定が入ると NAV だけ書き換わり **status は confirmed のまま・`detail.positions` は旧建玉のまま**(実測 10,010,000→10,009,900)。`_record_unrecorded_fills` は `entry_date=filled_at::date` で過去日付に書くため通常経路。是正: 差分に拠出資本以外を含む日は provisional へ降格しブレイク通知
- **再-3(重要)**: 再締めが `detail.assets/liabilities/positions` を更新しないため、証憑としての snapshot が自己矛盾する(実測 nav=15,000,000 / detail 由来 10,000,000)。`book_totals` は全項目を返しており追加コストはゼロ
- **再-4(重要)**: 変化検出が NAV 等値なので、手数料ゼロの遅延約定(NAV 中立)は `[]` を返し永久に取りこぼす(水位は 3325→3326 と動いている)
- **再-5(重要・肯定+未完)**: `input_refs` の水位設計は妥当で、`entry_id` が IDENTITY(単調)なので「後から立った仕訳を見たか」に厳密に答えられ、`entry_id > stored ∧ entry_date <= as_of` で遅延仕訳を**一意に列挙**できる(タイムスタンプより強い)。`code_version` を `meta.runs` から引く判断も正しい。**問題は読む側が無いこと** — 実測で窓外 D0 のみ `stale=True`・窓内3日は `False` と正しく判別できており、この1クエリ/日で再-1 は検出可能。逸脱3(窓外の先送り)はこの検出器を書けば不要
- **再-6(中)**: 逸脱1(過去日に `run_daily_close` を再実行しない)は**検証の結果正しい** — `_util.replay_position`(`_util.py:215-264`)は `entry_date` で絞らず全期間再生し、`post_mark_to_market`(`posting.py:298-316`)がその現在数量を使うため、過去日呼び出しはその日に存在しなかった建玉を過去日付で記帳する。帰結として遅延約定の日の NAV は原価のままで時価でない旨を明記すべき
- **再-7(中)**: 通知が弱い。確定値の書き換え(= restatement)が `_build_ops_embed`(`daily.py:379-404`)で ✅ 付き 1 行に埋もれ `[:1024]` で切られる — 重要-4 で指摘した欠陥の再現。ブレイクと同格の専用 embed にすること
- **再-8(中)**: `_sync_nav_daily_after_reclose` が `run_id` を差し替えるため nav_daily 唯一のリネージ(元の締めの run/code_version)が失われる。`detail.reclose.previous_run_id` を残すこと。また同じ日を 2 回書き換えると `detail.reclose` がまるごと置換され最初の `nav_before` が消える(実測 10M→15M→16M で 15,000,000 のみ残存)— `reclose` は配列で append すること
- **再-9(中・手続)**: `config/execution.yaml` は protected_areas 未登録だが `close.reclose_business_days` は会計エンジンの restatement 深度を決める値になった(`0` で是正が無言で無効化される)。登録するか窓の値を日次サマリに毎日出すこと。また `src/ryza/ledger/**` は保護領域で `3698ebe` に `Approved:` トレーラが無い — 本審査 → #承認 → 48h を経ずにマージしないこと
- **回帰テスト(シナリオ G)の実効性: 部分的**。修正前 `[-0.5,+0.5]` と修正後 `[0.0,0.0]` を **`book_returns` という観測量で**両方固定した点は正しい様式。ただし `test_reclose_window_bounds_which_days_are_rewritten` は窓外の日を NAV 値だけで固定しリターン系列を検査していないため、**再-1 の不具合を「仕様」として固定してしまっている**(同テストに `book_returns` の assert を足せば即座に赤くなる)。confirmed 日の建玉変化・detail 陳腐化・NAV 中立の遅延仕訳・2 回書き換えも未固定

## 再々審査記録(2026-08-04 独立役員 — 差し戻し後の限定スコープ再審査 `de1dad6` `8fd48b4`)

- 対象・手法: worktree 最終 `8fd48b4`。実 DB(`ryza_test`)にプローブ 11 本を書いて実測し、判定は全て `book_returns`(観測量)で行った。プローブは審査後に削除
- **再-1: 解消。** 20 日締めた後に 19 営業日前の日付で出資を立てる対照実験で、破損時 `[-0.5, 0, …]` → 再締め後 `[0.0]×20`(非ゼロ要素ゼロ)。19 日すべてが restated として列挙され、`age_business_days` は 19→1 と正しく減る。窓の不在もコードで確認 — `reclose_stale(book_id, through, run_id)` に N が無く、SQL の日付条件は `snap_date <= through` のみ、`close.reclose_business_days` は config から消滅
- **再-2: 判定は動くが述語が誤特定。条件付き。** 拠出資本のみ→フラグ無し / `post_fill` 混在→フラグ+`nav_daily` 伝播+`navflow` 経由の読み出し、いずれも実測で再現。「一度立てたら下ろさない」も確認(2 回目の再締めで戻り値は False でも `detail.recon_invalidated` は True のまま)— 証憑としてこの不可逆性は正しい
- **新-1(重要・両方向の誤判定)**: `non_capital_late` は**仕訳単位**で「拠出資本に触れない遅延仕訳があるか」を見るため、建玉を動かす仕訳に capital 行を 1 本混ぜると判定から消える。実測: 現物拠出型の 1 仕訳(securities 借/capital 貸)で NAV は 10,000,000→11,000,000 に restated されながら `recon_invalidated=False`・`status='confirmed'` のまま(建玉は増えている)。逆に建玉を一切動かさない費用アクルーアル(interest_expense/cash)は `True` になる。是正: 述語を**行レベル・建玉性**へ — 遅延仕訳のうち `jl.instrument_id IS NOT NULL` の行を持つものの存在を見る。実測でこの述語は 現物拠出=True / 費用アクルーアル=False / 通常の出資=False / 約定=True と 4 ケースを正しく分離する
- **新-2(重要・既決事項の再発)**: `recon_invalidated` が不可逆であるのに `run_risk_daily` の urgent が「フラグのある日が 1 日でもあれば urgent」なので、履歴のどこかで遅延約定が 1 度起きると**日次リスクレポートが永久に urgent かつ FLASH 色**になる。これは中-5(「pending 継続中は毎日 urgent・材料性しきい値なし」)として既に是正済みの欠陥の再発であり、材料性しきい値を入れた同じ裁定がここには適用されていない。是正: `detail` の永続化は不可逆のまま、**通知は鮮度・材料性で限定**する(当該再締めで新たに立った日、または測定窓内の日に限り urgent。それ以前は非 urgent の注記)
- **新-3(中・残存する偽リターン。既存不具合で回帰ではない)**: MTM を打ち直さない帰結として、遅延約定の日の建玉が取得原価で残り**恒久的な偽リターン**が立つ。実測: 市場価格 1200 の日に 1000 で 1000 株を遅延記帳 → 真値 `[0.0]` に対し観測 `[+0.020]`、翌日以降も訂正されない。振幅は「約定日の値洗い差 × 数量 / NAV」に限られ ±50% 級ではないが、`ewma_vol` 経由で重要-1 と同じ経路に乗る。`recon_invalidated` が当該日を名指しする点は防御として有効。抜本是正は `_util.replay_position` に `as_of`(`entry_date <=`)を足して過去日の MTM を再適用すること — リマインダー登録を要求する
- **新-4(低)**: 水位が NULL の日は fail-safe で恒久 stale になる。実測: 仕訳より前の日付のスナップショットは毎回の締めで再検出され、`producer_history` が締め 1 回につき 1 件ずつ無限に伸びる(5 回で 5 件・detail 1.4KB)。restated は立たないので通知は出ず、無言の無駄書きが続く。運用日付では起きにくいが、`stored_watermark IS NULL` かつ `current_watermark IS NULL` の日は対象から外すこと
- **新-5(低・計算量)**: stale 検出は各スナップショットごとに `journal_entries` の相関サブクエリを 2 本回す(`EXPLAIN ANALYZE` で `SubPlan 3` が `loops=15`)。スナップショット数 × 仕訳数で伸び、`ledger.journal_entries` に `(book_id, entry_date)` の索引も無い。年単位で運用すると毎日の締めが線形に重くなる。安価な枝刈り: 全スナップショットの `stored_watermark` の最大値より後ろの `entry_id` を持つ最古の `entry_date` を 1 回求め、その日以降のスナップショットだけを候補にする
- **再審査項目3(回帰テストの実効性): 解消。** 前回「NAV 値だけを固定していた」`test_reclose_window_bounds_which_days_are_rewritten` は削除され、`test_reclose_resolves_false_return_pair` / `test_reclose_catches_entries_older_than_any_fixed_window` が破損時 `[-0.5, 0, 0, 0]` と修正後 `[0.0]×5` を **`book_returns` で**両方固定している。NAV 中立仕訳の水位更新・再検出停止・建玉明細無効化・2 回訂正の痕跡連鎖も固定済み。未固定は新-1/新-2/新-3 の各ケース
- **起草者の留意点3点の独立評価**: (a)「前向きのみ」= **中**。`reclose_stale` の呼び出し口は `run_demo_close` だけで、`jobs/daily.py` の `_run_stage` は締め段の例外を握って後続を走らせるため、締めが落ちた日はリスク日次が**未再締めの系列を黙って測る**。リスク側に「直近の締めが成功したか」を渡すか、締め失敗日は測定を保留すること。(b)「測定除外なし」= **妥当。反対する点を探したが見つからなかった** — 除外は系列に穴を空けて隣接リターンを合成するだけで人工物を移動させるにすぎず、かつ「都合の悪い日を落とす」経路を作る。正しい是正は除外でなく新-3 の MTM 再適用。ただし `risk.limits_state_events.metrics` に「測定窓に照合無効日が n 日含まれた」を残していないため、`vol_exceeded` が汚染系列で立った事実が事後監査で再現できない(不変原則3)— `state_metrics` への 1 キー追加を要求する。(c)「limits_state 未接続」= **妥当**。翌日の締めが建玉照合をゼロから再実行するので無効化は過去日限定の事象であり、発注を止める根拠にならない。(b) の metrics 記録で足りる
- **手続(再-9 の残)**: `config/execution.yaml` から当該キーが消えたため即時の危険は解消し、`src/ryza/risk/**` の protected_areas 登録(重要-8)も確認した。ただし `de1dad6` `8fd48b4` はいずれも保護領域 `src/ryza/ledger/**`(および `src/ryza/risk/**`)を変更しながら `Approved:` トレーラを持たない。本審査 → #承認 通知 → 48h を経ずにマージしないこと
- **総合判定: 条件付き承認(追加是正要)。** 再-1 の中核(水位検出)は差し戻し理由を完全に解消しており、固定窓へ戻す提案には反対する。ただし `8fd48b4` で追加された `recon_invalidated` は新-1(建玉を動かす訂正を見逃す)と新-2(永久 urgent — 既決の中-5 の再発)を抱えており、いずれも SQL 述語と urgent 条件の小修正で閉じるため、**マージ前にこの 2 点を是正すること**を条件とする。新-3・新-4・新-5 は `ops/reminders.yaml` への登録で可

## MTM リプレイ審査記録(2026-08-04 独立役員 — 新-3 の是正 `83fc65a`)

- 対象・手法: `src/ryza/ledger/_util.py`・`closing.py`(**保護領域 ledger_engine**)・`execution/close.py`・`jobs/daily.py`。実 DB(`ryza_test`)にプローブ 10 本を書いて実測、判定は `book_returns` と `nav_snapshots` の実値で行った。プローブは審査後に削除(既存 186 テスト green・ruff clean)
- **新-3: 解消を確認。** d0 に 1000 株@1000 を遅延記帳し d0〜d3 の終値を 1200/1300/1400/1500 とした対照実験で、NAV は 10.2M/10.3M/10.4M/10.5M、returns `[+0.98%, +0.97%, +0.96%]` = 真値。**遅延日以降の中間日もすべて打ち直される**(水位が全日を stale にするため)。部分的な遅延売りも returns `[0.0, 0.0]` で真値
- **逆仕訳を as_of で切る判断: 正しく、かつ材料的。** カウンターファクチュアルを実測: 非対称版(逆仕訳を日付で切らない)は as_of=d0 で 数量 0 / 帳簿価額 50,000 → delta **−50,000**(取得原価全額の偽 NAV 下落)、対称版は +10,000。起草者の主張どおり
- **新-6(重要・要修正)**: `_historical_price_source` は `latest_close`(**on_or_before**)なので、その日のバーが欠測でも前日以前の終値を黙って返す。設計された縮退(`None` → `mtm_not_reapplied`)は「その銘柄のバーが 1 本も無い」ときしか発火せず、起草者の縮退テストはその特殊ケースだけを固定している(= 偽の安心)。実測: d(-1) 終値 900・d0 バー欠測 → `mtm_reapplied=True`・`price='900'`・`priced_at=d0`(**虚偽の証憑**)で NAV 9,900,000、returns `[-0.010, +0.030]`。しかも後から d0 のバーを取り込んでも当該日は stale でないため再締めは `[]` を返し**誤価格が恒久固定**される。是正: 実際に使ったバー日付を `detail.mtm_reapplied.price_bar_date` に記録し、`!= snap_date` なら `mtm_price_stale` を立てる(+ バー到着後の再訪をリマインダー化)
- **新-7(重要・要修正)**: 縮退は「維持」ではなく **revert**。実測: 一度 10,200,000 へ是正した日が、次の再締めで価格を引けないと 9,999,999(原価)へ戻り偽 +2% が復活する。docstring の「呼び出し側が `mtm_not_reapplied` を維持する」は事実と異なる。是正: 前回 `mtm_reapplied.delta` があり今回再評価できない日は前回 delta を明示フラグ付きで引き継ぐか、NAV を書き換えない
- **新-8(中)**: `price_source` の既定が `None` のため、渡し忘れた呼び出しは是正済みの日を黙って原価へ戻す(実測 NAV 10,200,000→9,999,999・returns `[+0.020]` 復帰)。現在の呼び出し口は `run_demo_close` 1 箇所で潜在だが、対照テストはこの経路を「仕様」として追認している。是正: キーワード必須化、または `detail.recon_invalidated` を持つ日を価格ソース無しで再締めしようとしたら失敗させる
- **新-9(中・「仕訳を書かない」設計の帰結 = 判断3 の独立評価)**: 方針自体は支持する(過去日記帳は水位を進めて自己 stale 化する — 再-6 の理由は生きている)が、`detail.assets/net_income` を**評価替え後**の値で上書きするため(`closing.py:519`→`612`)、複式簿記で最も強い監査不変式「試算表合計 = NAV」がスナップショットから検証できない。将来の NAV 独立再計算監査は再締め日を偽ブレイクとして出す。是正は 1 行: 調整前の `book_totals["nav"]` を `detail.nav_from_journals` に残し、`nav = nav_from_journals + mtm_reapplied.delta` を照合可能にする。これで不変原則3 を満たす
- **新-10(重大・既存不具合。本 PR の回帰ではないが `_reapply_mtm` が同じ欠陥を複製)**: 全売却で `securities` に前日 MTM の残渣が残り NAV が恒久的に過大になる。`run_daily_close`(`closing.py:173-174`)も `_reapply_mtm`(`406-407`)も `qty == 0` を評価替えの**前**にスキップするため、建玉ゼロの銘柄が時価ゼロへ洗い替えられない。実測(origin/main でも同一): 1000@1000 買い → 終値 1200 で評価替え → 翌日 1200 で全売り → `securities` 残高 200,000・数量 0、NAV 10,200,000→10,400,000、returns `[+0.0196, 0.0]`(真値 `[0.0, 0.0]`)= **新-3 と構造的に同一の恒久的偽リターン**。本 PR 固有の害は、遅延全売り日の再締めが `mtm_reapplied=True` と「その日の終値で評価替えして NAV に反映した」注記を付けながら 1 銘柄も評価替えしない点(`positions={}` で残渣は資産に残る)。是正: `qty == 0` でも評価替えを打つ(delta = 0 − 帳簿価額)。両呼び出し口を同時に。別 PR(保護領域・独立審査要)+ リマインダー登録を要求する
- **新-11/新-12(低)**: `_sync_nav_daily_after_reclose`(`close.py:321-323`)は `mtm_reapplied` を True にするだけで戻さないため、再適用が外れた日で ledger 側(`pop` する)と二表のリネージが食い違う。`daily.py:381-383` の警告は**今回の run の** `recon_invalidated` で分岐するので、2 回目以降の再締めで再適用に失敗した日は警告が一切出ず、文言も `price_source` 未指定のケースを誤説明する
- **判断2(`positions_stale` 維持・復元値は `mtm_reapplied` 配下): 支持。覆す価値なし** — `mtm_not_reapplied` が「原価か時価か」を別キーで持つので語彙の分離で情報は失われない。**判断4(部分適用しない): 支持。ただし前提が成立していない** — 「欠測なら `None`」という前提が `_historical_price_source` で破れている(新-6)。全か無かの門は「価格の有無」でなく「**その日の**バーの有無」で切るべき
- **対照テストの実効性: 部分的。** 修正前挙動を `nav_d1/nav_d0 - 1 == 0.02` で固定した様式は正しく、execution 側は `book_returns` で観測量固定しており適切。ただし固定対象が「修正前のコード」でなく「`price_source` 無し経路」であるため新-8 を仕様化しており、縮退テストは新-6 の現実的な欠測を覆っていない
- **手続**: `83fc65a` は保護領域 `src/ryza/ledger/**` を変更しながら `Approved:` トレーラを持たない。本審査 → #承認 通知 → 48h を経ずにマージしないこと
- **総合判定: 条件付き承認(追加是正要)。** 新-3 の中核は実測で解消しており、`as_of` リプレイ・逆仕訳の対称カットという設計選択に反対する点は無い。**マージ前に新-6・新-7 を是正すること**(いずれも数行。どちらも「恒久的な偽リターンを消す」という本 PR の目的を別経路で破る)。新-8・新-9 は同 PR 内が望ましい。新-10 は別 PR、新-11・新-12 は `ops/reminders.yaml` 登録で可

## 残渣修正審査記録(2026-08-04 独立役員 — 新-10 の是正 `4ec80c1`)

- 対象・手法: `src/ryza/ledger/posting.py`・`_util.py`・`closing.py`(**保護領域 ledger_engine** — `config/governance.yaml:97`)。実 DB(`ryza_test`)にプローブ 14 本を書いて実測し、判定は `book_returns`・`securities` 残高・証憑 payload の実値で行った。プローブは審査後に削除(既存 406 テスト green・ruff clean)
- **新-10: 解消を確認。** 審査実測ケースを両方向で固定した — 修正前 NAV 10,200,000→10,400,000 / `securities` 200,000 / returns `[+0.019608]`、修正後 10,200,000→10,200,000 / 残高 0 / `[0.0]`。`origin/main` の src に差し替えると起草者の新規テスト 6 本が落ちるので**対照テストは実効的**(観測量で固定している)
- **起草者の逸脱(`mtm_book_value`)は正しい。処方どおりの delta = 0 − 総額に反対する。** 「売りが取り崩さなかった未実現益」との厳密一致を 3 系列で実測: 分割売り+中間評価替え(returns `[+0.009804, +0.005825]` = 真値)・含み損での全売り(`[-0.010204]`)・益→損の往復(`[-0.029412, 0.0]`)、いずれも `securities` 0 / `unrealized_pnl` 0 / 実現損益は移動平均法の真値。現物拠出+約定+評価替えの混在でも評価替えぶん(200,000)だけが消え拠出ぶん(300,000)は残る。逆仕訳された評価替えは `securities_book_value` と対称にゼロになる。**不採用代替案(post_fill での時価取り崩し)の棄却理由も妥当** — 実現損益が `gross − 時価 = 0` になり移動平均法の実現/未実現分離が壊れる。反対すべき点を探したが見つからなかった
- **`price=None` 経路: 支持。** 建玉ゼロの時価は価格に依らずゼロだから終値を引かない設計は正しく(上場廃止・バー欠測で締めを落とさない)、証憑に `price=null` + `zero_qty_writeoff` を書く判断は新-6 の教訓と整合する。`qty>0` の ValueError ガードも実効的だった — 私が新-13 の是正を片側だけ当てたとき、沈黙して 0 評価せず即座に落ちた
- **新-13(重大・本 PR の回帰。マージ前に是正)**: 将来日付の売り約定が先に記帳されている日の締めが、**建玉を持つ日の MTM 残高を洗い替える**。`run_daily_close`(`closing.py:183`)も `post_mark_to_market`(`posting.py:332`)も数量を **as_of なしの全期間再生**で取り、帳簿価額だけ `as_of=entry_date` で取る非対称(再-6 で既知)を、本 PR が「数量ゼロ ⇒ 残渣を消す」の判定に使ってしまった。実測: d0 に 1000@1000 買い → 終値 1200 で締め → d2 付けの全売りを先に記帳 → **d1 の締めが NAV 10,200,000 を 10,000,000 に落とし returns `[-0.019608]`**(真値 `[0.0]`)。**修正前は同じシナリオで 10,200,000・`[0.0]` = 正しかった**ので純粋な悪化であり、しかも符号が逆なだけで新-10 と同じ恒久的偽リターンである。是正は 2 行(実測済み): `closing.py:183` に `as_of=date`、`posting.py:332` に `as_of=entry_date`。適用後 returns `[0.0]`・NAV 10,200,000 に戻り、既存 406 テストは全 green。**片方だけ当てると ValueError で落ちるので同時に**
- **新-14(重大・敵対的。洗い替えは双方向に NAV を書く原始操作)**: 消す額の判定子が evidence の自由文字列 `kind = 'price_snapshot'` のみ(`_util.py:350`)であり、記帳元の性質を見ていない。実測(a) `kind='price_snapshot'` を付けた手仕訳 Dr `securities` 3,000,000 / Cr `capital` は、次の締めで**実在資産を全額消され** NAV 13,000,000→10,000,000、`unrealized_pnl` に偽の 3,000,000 損失が立ち、証憑には「建玉ゼロ — 全売却後の残渣を洗い替え」と記録される。実測(b) 同じ kind で貸方に 500,000 を立てると逆に NAV を無から 10,000,000→**10,500,000** に増やし偽の未実現益を計上する。会計の別経路(`realized_pnl`)は汚れないが、`unrealized_pnl` は `net_income`→NAV→`book_returns`→`ewma_vol`→`vol_exceeded` と本 PR が守ろうとしている経路そのものに乗る。是正: 判定子に `journal_entries.posted_by`(`0005_ledger.sql:45`。既存列)を加える、より根本には **MTM 層を独立勘定に分けて構造で切る**(推定をやめる)
- **新-15(重要・説明不能な残渣の検出器が無い)**: 洗い替えは `mtm_book_value` ぶんしか触らないので、それ以外の理由で数量ゼロなのに残る `securities` を**誰も見ていない**。実測(a) 買いだけを逆仕訳して売りを残すオペミスで `securities` = **−1,000,000**・数量 0 のまま締めは無言で通り、試算表もゼロバランスなので気づけない。実測(b) 評価替えと同形だが `kind='decision'` の手仕訳が作った残渣は `mtm_book_value=0` のため洗い替えられず、returns `[+0.019608]` が恒久残存する — **新-10 と同一の偽リターンが別経路で完全に再現する**。なお `recon.reconcile` の valuation 突合は修正前にこれを捉えていた(実測 ours 200,000 / theirs 0 → `break_open`・NAV は provisional 据え置き)ので、同じ検査(数量ゼロ ⇒ 約定外建玉を除いた `securities` はゼロ)を締めに内在させれば足りる。ブローカー snapshot が無い経路では recon が走らないため第二防衛線も消える
- **新-16(中・語彙の不一致)**: 同じ事象に 2 つのスキーマがある — 当日経路は `detail.zero_qty_writeoffs`(`closing.py:227`)、再締め経路は `detail.mtm_reapplied.positions` の中に `{"qty":"0","price":null,"zero_qty_writeoff":true}` を混ぜる(`closing.py:467`)。実測で確認。`positions` を建玉明細として読む下流は再締め経路でだけ数量ゼロの幽霊行を見る。NAV は両経路で一致(実測: 当日締め・再締め・再々締めのいずれでも 10,200,000、2 回目の再締めは `[]`)しており、審査が要求した「両呼び出し口を同時に」は満たされている
- **新-17(低・前提の脆さ)**: 現物拠出の `securities` は**一度も評価替えされない**(実測: 価格 1500/2000/500 を渡しても残高 1,000,000・NAV 11,000,000 のまま、`positions` も空)。本 PR が「約定外の建玉に触れない」で正しくいられるのはこの前提のおかげであり、根因は `replay_position` が建玉の唯一の真実でないことである。将来 in-kind を時価評価する必要が出た時点で本修正の前提は崩れる
- **手続**: `4ec80c1` は保護領域 `src/ryza/ledger/**` を変更しながら `Approved:` トレーラを持たない(コミットメッセージ自身がそう明記している)。本審査 → #承認 通知 → 48h を経ずにマージしないこと
- **総合判定: 条件付き承認(追加是正要)。** 新-10 の中核は実測で解消し、`mtm_book_value`(評価替えが作った残高だけを消す)という起草者の逸脱は前回処方より正しい — 覆す価値は無い。ただし**新-13 はマージ前に是正すること**(2 行。修正前より悪化しており、本 PR の目的を符号違いで破る)。新-14・新-15 は同 PR 内が望ましく、少なくとも `ops/reminders.yaml` 登録を要求する。新-16・新-17 はリマインダー登録で可

## MTM 勘定分離審査記録(2026-08-04 独立役員 — `674e8fa` / migrations 0034 + 現物拠出)

- 対象・手法: `migrations/0034_ledger_mtm_account.sql`・`src/ryza/ledger/{_util,posting,closing}.py`・`docs/design/11-mtm-account-separation.md`(**保護領域 2 つ: 会計エンジン + migrations**)。新規 DB でプローブ 7 本を書いて実測し、判定は NAV・勘定残高・`unexplained_residue` の実値で行った。プローブは審査後に削除(新規 DB `ryza_io_review` で全 1837 件 pass、共有 `ryza_test` の 6 件失敗は並行ラインの outbox 制約による環境汚染で本 PR と無関係)
- **移行コストゼロの主張: 独立に再現。反対すべき点を探したが見つからなかった。** 本番 DB を自分で数えた — `journal_entries` 2 行 / `journal_lines` は `cash` 2・`capital` 2 のみ(`securities` 明細 0)/ `nav_snapshots` 0 / `meta.schema_migrations` 最大 `0015`。追記オンリー(0005)ゆえ履歴が積まれた後は再分類仕訳しか手段が無く必ず `reclose_stale` に掛かる、という非対称の論証も妥当。採番 0034 の歯抜けもランナーが集合判定(`migrate.py` `if version in done`)なので 0030–0033 は後から適用される — 実測で確認、問題なし
- **消費者不変(`securities_book_value` の意味保存): 成立を確認。** `recon.reconcile` は同関数経由、`statements.book_totals` は category 駆動、`cash_flow` は現金を含む仕訳だけを集計するので MTM は元から対象外、`dashboard/` は `securities` 参照 0 件。等価性の対照群(新-10/新-13/新-16 の NAV・returns 固定テスト)は**無改変で pass**しており、`execution` 側も合計 100,000 を維持したまま内訳(原価 100,064 / 評価調整 −64)に分解しているだけで緩和ではない。**新-17 は解消**(拠出 1000@1000 が終値 1500 で NAV 11,500,000・`positions` に出る)
- **新-18(重大・マージ前に是正)**: DB トリガ `check_mtm_line` の逆仕訳免除が `reversal_of IS NOT NULL` の**フラグだけ**で決まる。`reversal_of` は `post_entry` の公開引数であり、対象仕訳の内容とも帳簿とも突合されない。実測: `posted_by='probe.attacker'` のまま無関係の entry_id を `reversal_of` に入れて `Dr securities_mtm 3,000,000 / Cr capital` → **トリガ通過**、次の締めが全額洗い替えて NAV 13,000,001→10,000,001、偽の未実現損 3,000,000、`unexplained_residue` は**空**、`zero_qty_writeoffs` には正規の洗い替えとして記録された(新-14(a) が posted_by 検証を迂回して完全再現)。是正: 免除条件を「対象仕訳が同一 book かつ `securities_mtm` 明細を持つ」に狭める(トリガ内 1 クエリ)、または `post_entry` の `reversal_of` を `reverse_entry` 専用にする
- **新-19(重要・主張と実装の矛盾。マージ前に文言是正)**: 「原価恒等式が新-14/新-15 を覆う」は**評価調整勘定を直接叩く偽装には成り立たない**。実測: `posted_by='ledger.closing'` を騙って `Dr securities_mtm 3,000,000 / Cr capital` → NAV 13,000,000→10,000,000、`unexplained_residue` は空(完全に無音)。設計文書 §5.2-1 は正直に「塞がない」と書いているのに、`closing.py` のコメント・commit message・`ops/reminders.yaml` の完了記述は「評価替えを騙る手仕訳は恒等式を破る側に落ちる」と書いており、文書が自ら §1 で最重要と位置づけた誤読をコード側が作っている。恒等式が覆うのは**原価勘定側の**直接記帳だけである
- **新-20(重要・同 PR 内が望ましい)**: 恒等式の視界は `held_instruments`(instrument_id 付き明細)に限られ、`securities` には 0034 が `securities_mtm` に課したような instrument_id 必須ガードが無い。実測: `Dr securities 2,000,000 / Cr capital`(instrument_id NULL)は `held_instruments()==[]` で恒等式に一度も掛からず、NAV 12,000,000 が無言で恒久残存する。新-15 の「誰も見ていない」は未解消。是正は 0034 と同型のガードを `securities` にも張ること(instrument_id 必須 + 親証憑 kind ∈ `POSITION_EVIDENCE_KINDS` または逆仕訳)であり、reminder が挙げた選択肢①と同じ効果を持つ
- **新-21(重要・本 PR が新設した増幅経路)**: `post_in_kind_contribution` は誰でも呼べる NAV 生成プリミティブで、証憑には自分が書いた数量・単価がそのまま入るため**恒等式は構造的に必ず成立する**。実測: `posted_by='probe.attacker'` で 1000@1000 を拠出 → 終値 3000 で締め → NAV 13,000,000・`unexplained_residue` 空。新-17 是正の副作用として、架空建玉が**評価替えにも乗る**ようになった(是正前は原価のまま動かなかった)。恒等式は「同一呼び出しが書いた 2 つの成果物の内部整合」であって実在性の検査ではない — §3.2 の「約定を経ない直接記帳も同じ 1 本の検査で名指しできる」は API を使わない場合に限る。是正案: 拠出に `posted_by` 制限か出資契約証憑の参照を要求し、締めで in-kind 由来建玉を明細化して通知する
- **新-22(中・偽陽性)**: 健全な帳簿でも後日付の約定が先に記帳されている日は `cost_identity_broken` が鳴る。根因は `post_fill` の売りが **`as_of` なしの全期間再生**で `cost_released` を決める(`posting.py:266`)一方、恒等式は `as_of=date` で切ること — 新-13 で是正した非対称が `post_fill` に残っている。実測: d0 買い 100@500 → d2 買い 100@700 を先に記帳 → d1 売り 50@800 → d1 の締めが `{book_value: 20000, replay_cost: 25000, qty: 50, cost_identity_broken}`(d2 の締めでは消える)。この日は #運営 に ⚠️ が飛ぶ。毎日の通知に載せる検査である以上、偽陽性の第一の源は先に潰すこと
- **新-23(中)/新-24(低)**: (23) 0034 の勘定追加は適用時点の `book_type='fund'` への一回限りの INSERT であり「将来の LIVE_FUND もここに乗る」(`0034:47`)は**誤り** — 0006 の 18 科目を写した将来のファンド帳簿では最初の評価替えが `ValueError: 未知の勘定科目: …securities_mtm` で落ちる(実測)。fail-loud だが LIVE_FUND は実弾(定款第3条)であり初日に締めが落ちる設計にはしない。(24) トリガが `'ledger.closing'` をハードコードし `_util.MTM_POSTED_BY` と二重管理 — 2 つ目を足すと Python は通し DB が `RaiseException` で拒否する(実測)。「`MTM_POSTED_BY` の全要素が DB に受理される」テストを 1 本置けば足りる
- **設計文書 11: 執筆規格に適合。** アーギュメント先行・シノプシス・反対意見書(top3 + 各対処)・レベル1の出典いずれも満たす。§5.2-1 の自己反論は私の実測と一致しており、起草者は分離が新-14 を塞がないことを正確に理解している — 矛盾しているのはコード側の文言だけである(新-19)
- **手続**: `674e8fa` は保護領域 2 つ(`src/ryza/ledger/**` + `migrations/**`)を変更しながら `Approved:` トレーラを持たない。本審査 → #承認 通知 → 48h を経ずにマージしないこと
- **総合判定: 条件付き承認(追加是正要)。** 分離そのものには反対しない — 移行コストの非対称は実測で成立し、消費者不変と等価性も確認できた。ただし**マージ前に新-18(トリガの逆仕訳免除)と新-19(コード側文言の矛盾)を是正すること**。新-20・新-21 は同 PR 内が望ましい(新-20 はトリガ 1 本で新-21 の一部も塞ぐ)。新-22・新-23・新-24 は `ops/reminders.yaml` 登録で可
