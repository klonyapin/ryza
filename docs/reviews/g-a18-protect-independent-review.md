# 独立役員意見書(事後復元)— 保護領域拡充+A-18 改番(PR #66・g-a18-protect)

> **本書は原本ではない(事後再構成)**: PR #66 の独立役員審査は行われたが、その意見書は
> docs/reviews に保存されないまま起草セッションの終了とともに失われた
> ([07-development.md](../design/07-development.md) §3-1 違反)。本書は現存する二次記録
> — PR #66 本文・是正コミット f243fc1・審査対象コミット c64be99/9338398・マージコミット
> 1311800 — から 2026-08-03 に再構成したものであり、**審査時の原文ではない**。
> 「(復元)」を付した記述は二次記録からの推定を含む。復元できないものは
> §復元の限界 に列挙した。本書を審査当時の逐語として引用しないこと。

- 審査日: 2026-08-03(9338398 21:23 JST 以降、是正 f243fc1 21:37 JST 以前)/ 復元日: 2026-08-03
- 対象: PR #66(branch g-a18-protect)= c64be99(ops/deploy-dashboard.sh の保護登録・L1)+ 9338398(定款監査 A-13 → A-18 改番・Issue #48)
- 審査者: 独立役員(非執行・批判専任・プロンプト分離)。原記録喪失のため審査セッションは同定不能
- 復元者: 設計リード。各記述に復元根拠(PR 本文/コミットメッセージ/コミット差分)を付す

## 判定: 条件付き承認 C-2〜C-6 → 全条件反映済みで統合

- 承認単位は2つに分離(C-2): ①L1 = config/governance.yaml の保護領域拡充 ②L4 = 監査番号改番。どちらか一方のみの否認も可能(根拠: PR 本文冒頭)
- 統合: マージコミット 1311800(みなし承認 — #承認 通知 2026-08-03・`Approved:` トレーラあり・独立役員審査は L1 レビュー兼)

## 審査が行った検証(復元)

- **改番の無改変性を blob バイト一致で実証**: a13.py → a18.py 等の改名が検査ロジックに改変を混入させていないことをバイト同一性で確認した(根拠: PR 本文「検査ロジック無変更 — 独立審査が blob バイト一致で改変混入なしを実証」)。比較の具体手順(対象ファイルの範囲・rename 検出の方法)は現存記録になく復元不能

## 条件と是正(指摘=復元、是正=コミットに現存)

### C-2: 承認単位の明示と、起票時裁定の履歴復元
- 指摘(復元): 本 PR は審査水準の異なる2承認単位(L1 保護領域拡充/L4 改番)を含む。PR 本文でこれを明示し、片方のみの否認を可能にすること。また governance-protect-deploy-dashboard リマインダーの done 化により、起票時の設計リード裁定「0020 の PR とは別 PR にするのは、承認の単位を『スキーマ』と『統制対象の定義変更』で混ぜないため」が body から消えた — 履歴として復元すること
- 是正: PR 本文冒頭に承認単位を明示。f243fc1 で裁定文言をリマインダー body に復元

### C-3: 既知違反の受容リマインダー化
- 指摘(復元): deploy-dashboard.sh の保護登録により、是正不能な歴史的 evil merge 2件(2191285・7a56c98)が A-18-1 の週次検査で恒久的に違反表示される。恒久警告は「毎週赤いが誰も見ない」形骸化(05-governance §6-5)を招く — 受容を機構(acknowledged_findings)で明示・可視化すること
- 是正: f243fc1 で `a18-acknowledged-findings` リマインダー登録(期限 2026-08-10)。機構の実装は src/ryza/audit/**・governance.yaml とも保護領域のため別 PR・L1 手続とした

### C-4: 旧ユニット撤去手順の強化
- 指摘(復元): 改番マージ直後から旧 ryza-a13 系 systemd ユニットが毎週失敗する。撤去を強化し、再デプロイの確認点を次の週次実行より前に置き、マージ直後に実走確認すること
- 是正: f243fc1 で deploy-a18.sh の撤去ブロックに `systemctl reset-failed` を追加、`a18-vm-wiring-check` の date_after を 2026-08-17 → 2026-08-05 に前倒し。PR 本文にマージ直後の `./ops/deploy-a18.sh` 実走確認を明記(「審査 C-4 条件」)

### C-5: 「保護登録が実体を失う」経路の機械的検知
- 指摘(復元): 本改番の改名(deploy-a13.sh → deploy-a18.sh 等)で、保護領域の登録パスが実体を指さなくなり A-18-1 が静かに無力化する経路が現実の危険として顕在化した。非 glob パスの実在を不変条件テストで固定すること
- 是正: f243fc1 で `protected-areas-path-existence-test` リマインダー登録(期限 2026-08-10)。tests/test_ips.py 自体が保護領域のため別 PR・承認手続とした

### C-6: 宙吊り参照の修正
- 指摘(復元): 改番・改名で生じた参照の不整合3点 — ①07-development.md のリンク表示が実ファイル名(a13-audit-independent-review.md)と乖離 ②a18-implementation-checkpoint に旧 id(a13-*)からの改名注記がない ③governance-protect-deploy-dashboard の行末コメントが途中で切れている
- 是正: f243fc1 で ①「A-13 審査(現 A-18)」表記に修正 ②改名注記を追記(docs/minutes の旧 id 参照は歴史的記録として据え置く旨も明記)③コメント完結

### 条件外の追加是正(f243fc1 に同梱)
- protected_areas に `ops/lib/**`(area: deploy_path)を登録 — デプロイゲート関数の移設先(並行ラインで新設)。スクリプト本体のみの保護ではゲート関数側から統制を外せるため。コミットでは「追加」と表記され条件番号がなく、審査の指摘に由来するか設計リードの自発かは現存記録から判別できない

## 復元の限界(失われたもの)

- **C-1 の有無・内容**: 現存記録の条件番号は C-2 から始まる。C-1 が存在したのか(存在した場合、是正不要と裁定されたのか・取り下げか・単なる欠番か)は復元できない
- **重大度区分**: 他の意見書が付す重大/中/低の区分は現存記録にない
- **「妥当と認めた点」**: 承認理由の積極的記述は PR 本文の要約(裁定文言の復元・既知違反の受容リマインダー化・撤去手順強化・宙吊り参照修正)以上に復元できない
- **反対意見書・審査と是正の往復**: CLAUDE.md 議論規約2 の反対意見書が添付されたか、07-development §4-1 が求める往復の逐語は失われた
- **メトリクス集計上の扱い**(§3-3): 本審査は「指摘 5 件(C-2〜C-6)+有無不明 1(C-1)・重大度不明・是正率 5/5」として扱い、重大度別集計からは除外すること

## 原本喪失の経緯と再発防止

- 経緯: 審査は起草セッション内で行われ(プロンプト分離)、結果は PR 本文とコミットにのみ記録された。意見書ファイルの保存(§3-1 の義務)が漏れたまま統合・セッション終了に至った
- 再発防止: §3-1 の執行点「審査なし統合を A-18 拡張で検出」(執行点対応表 — 宣言・要実装)を実装する際、**「審査はあったが意見書ファイルがない」本件同型も検出できるよう、docs/reviews の意見書ファイル実在検査を含めること**。この将来アクションは `ops/reminders.yaml` の `a18-review-opinion-file-check` に登録した(セッション内の約束は無効 — CLAUDE.md)
- 本書自体の位置づけ: 事後再構成は原本保存の代替にならない。以後の審査は意見書の保存を統合の前提とする

## 改訂履歴

- 2026-08-03 事後復元版 起草(設計リード。原本は喪失)

## 承継ルール審査記録(2026-08-04・独立役員 / 原本)

- 対象: A-18-1 の **PR 承継**(`src/ryza/audit/a18.py` L254-288)・`acknowledged_findings`(L315-361)・保護領域の残登録(`config/governance.yaml` L145-149)・パス実在テスト(`tests/test_ips.py`)。実走: 統合後を模した clone で violations=0 / inherited=3 / acknowledged=1 を再現、新旧テスト 92 件 pass
- **妥当と認めた点**: 承継が免除する差分(ブランチ内 evil merge の解消結果)は PR の三点差分 base…head に必ず現れることを実測で確認した。したがって「未レビュー内容の免除」ではなく、真に未レビューな first-parent マージ自身の解消差分はトレーラ必須のまま残している(L284-288)区別と整合する。受容機構の fail-safe(完全 SHA+パス集合の完全一致・不完全エントリ無効・陳腐化の notes 開示)も妥当。「main 到達前の草案取り下げは追記オンリー違反でない」という整理も是認する — 追記オンリーは発効済み記録の規律であり、未発効の推敲に及ぶと解すべきでない
- **重大-1(承継の起点が検証不能)**: `has_approval_trailer` は `^Approved:\s*\S+`、`_PR_MERGE_RE` は番号の実在を見ない。PoC で **実在しない PR #999999** の件名+架空 URL のトレーラを持つマージが、ブランチ内 evil merge による Kill Switch 改変(`LIMIT = 999999`)を violations=0 で通した。承継は偽トレーラ1件の爆風半径を「1コミット」から「ブランチ全体」へ公式化する。さらに実データ上、承継が救済した3件(PR #58/#70/#73)のトレーラは「みなし承認・#承認 通知 2026-08-03」と称するが、マージは同日 19:06/22:07/22:17 で 48h 未経過であり、記載どおりのみなし承認は成立し得ない
- **重大-2(審査後 push の吸収)**: 承継は「マージ時点のブランチ全体」に及び、独立審査・#承認 通知の後に積まれたコミットも同じトレーラで承認扱いになる。是正(重大-1 と共通): トレーラ様式を `Approved: <url> reviewed=<sha40> mode=deemed|principal notified=<ISO8601>` に拡張し、承継を `reviewed` の祖先に限定、`mode=deemed` は merge 時刻 − notified ≥ 48h を機械検査、様式不備は承継の起点にしない
- **中-3(集計が承継総量を隠す)**: `inherited` は「救済されたコミット」のみで、より広い附則(b)(L263-264)の通過量を測らない。octopus マージ(親4)に PR 件名を付けた PoC では未審査の保護ファイル3件が入って **violations=0・inherited=0・direct_pushes=0**。是正: 承継フィールドに保護パスの和集合を表示、(b) 通過も `pr_covered` として同様に可視化、octopus は起点から除外(GitHub の PR マージは常に親2)
- **中-4(「必ず可視化」が経路依存)**: `has_findings`(L543-545)は acknowledged/inherited を含まず、`run_and_report`(L689)は `always_report` 時のみ報告する。VM 経路(`ops/deploy-a18.sh` L86)は `--always-report` だが `ops/weekly.py` L357 は渡さない。全クリーン(=本変更が作る定常状態)では受容も承継も一切出ない。状態行(L361-364)にも両件数がない
- **中-5(棚卸し未完)**: `.github/workflows/ci.yml` が未登録。本 PR の不変条件テストを含む全テストの唯一の執行点かつ required status check であり、`Run tests` を削るだけで統制が静かに外れる(`ops/lib/**` を登録した論法と同じ)。`site/Dockerfile`・`compose.yaml` の対象/対象外判断も記録がない
- **中-6(CI で必ず失敗)**: 新規 `test_real_repo_acknowledged_findings_are_matched` は実リポジトリに `run_a18` を呼ぶが、`actions/checkout@v4` は fetch-depth 未指定=1 で批准コミット c7af81e が存在せず ValueError。`--depth 1` clone で再現済み。是正: ci.yml に `fetch-depth: 0`(中-5 の登録と同時に)
- **低-7/8/9**: ①`acknowledged_index` が 40 桁 hex を検証せず、短縮 SHA 登録は永久不一致+陳腐化注記のみ(運用者が受容できたと誤認しうる)②受容エントリの `reason`/`approval_ref` の**書換**は機械的に検出されない(削除は違反復活で自己是正的)③`_find_introducing_merge` の到達可能性計算は入れ子マージを含め意味論的に正しいが O(N·M) の subprocess 呼び出しで履歴長に対し二次
- **判定: 条件付き承認**。中-6 は統合前必須。中-3・中-4・中-5 は本 PR 内で低コスト是正。重大-1・重大-2 はトレーラ様式強化として同一 PR かフォローアップ PR とし、後者なら `ops/reminders.yaml` への登録を統合の条件とする(セッション内の約束は無効)
- **反対意見書(この提案が間違っている場合の理由トップ3)**: ①承継は「統合フローが違反を量産する」運用論で正当化されているが、正しい根拠は「承継対象は必ず PR 差分に現れる」であり、根拠を書き違えたまま拡張すると次の緩和(例: squash 対応・非 PR マージへの拡張)で歯止めを失う ②A-18-1 の緑は「保護領域が守られた」ではなく「トレーラが1行あった」を意味するに過ぎず、緑の恒常化は 05-governance §6-5 の形骸化を別経路で再生産する ③是正不能な過去の違反への対処は受容と承継の二機構に分かれ、どちらで処理するかの基準が governance.yaml の履歴注記にしかない。代替案: 承継は `reviewed` SHA 固定を伴う場合のみ有効とし、それ以外は受容(明示・別枠可視化)へ寄せる

## 設計リード裁定(2026-08-04)

- **重大-1 の一部を不採用**: 「PR #58/#70/#73 のトレーラは 48h 未経過であり記載どおりのみなし承認は成立し得ない」は**事実誤認**として採らない。定款 v0.4 第3条のみなし承認は「通知と同時に発効」(`config/governance.yaml` `deemed_approval.effective: on_notice`)であり、#承認 通知と同日のマージは正規の手続である。審査者は v0.3 の 48h 制を参照している。**ただし同じ重大-1 のうち偽 PR トレーラの PoC(実在しない PR 番号+架空 URL で承継が成立する)は正当な指摘として採用**する
- **重大-2 は採用・フォローアップ**: 承継が「審査後 push」を吸収する問題は、トレーラ様式の拡張(`reviewed=<sha40>` 等)を要するため本 PR の範囲外とし、`ops/reminders.yaml` の `approved-trailer-v2`(期限 2026-08-10)に登録した。統合の条件である「セッション内の約束は無効」を満たす。ただし `mode=deemed` の機械検査は「48h 経過」ではなく「#承認 通知の実在」を照合する形に読み替える(上記の理由)
- **本 PR 内で是正**(いずれも同 PR で実装):
  - 中-6(必須): `.github/workflows/ci.yml` の checkout に `fetch-depth: 0`
  - 中-3: 承継起点と附則(b)の対象を**親2のマージに限定**(octopus 排除)。承継の集計行に免除した保護パスの和集合を表示
  - 中-4: `src/ryza/ops/weekly.py` の A-18 呼び出しに `always_report=True`、状態行に受容・承継の件数を追加
  - 中-5: `protected_areas` に `.github/workflows/ci.yml`(`area: ci` — 新設)を追加。`site/Dockerfile`・`compose.yaml`・`ops/fetch-fonts.sh` を対象外と判断した根拠を governance.yaml のコメントに記録
  - 低-7: `acknowledged_index` が 40 桁 hex 以外のエントリを「無効(短縮 SHA 不可)」として notes 開示
  - 低-8: 受容エントリの `reason`/`approval_ref` 書換は一致キーに影響しないが、本ファイルが保護領域であるため書換コミット自体が A-18-1 の突合対象になる、という限界と担保を governance.yaml に明記
- **低-9(`_find_introducing_merge` の O(N·M))は据え置き**: 現状の履歴長(約 200 コミット)では実測で問題にならず、最適化はキャッシュ導入で監査ロジックを複雑にする。履歴が数千規模になった時点、または週次実行が体感で遅くなった時点で再評価する
- **反対意見書への応答**: ①「承継の正しい根拠は『承継対象は必ず PR 差分に現れる』である」を全面的に受け入れ、以後の緩和提案(squash 対応・非 PR マージへの拡張)はこの基準で判定する ②「緑は『トレーラが1行あった』を意味するに過ぎない」も是認し、だからこそ `approved-trailer-v2` を実弾移行前の必須項目として扱う ③受容と承継の使い分けは「承継 = 承認済み PR の差分に現れるもの / 受容 = 是正不能かつ承認に紐づかないもの」を基準とし、`approved-trailer-v2` 実装時に定款側へ明文化する
