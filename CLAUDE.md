# CLAUDE.md — Ryza プロジェクトの LLM 向け必読事項

このリポジトリは自動運用システム Ryza。作業前に必ず以下を把握すること。

## 正となる文書

- 全体設計: `docs/design/00-system-design.md`(v3.2)— 組織・原則・データ契約はここが正
- 執筆規格: `docs/design/70-writing-standard.md` — **保護領域。変更はユーザー承認必須**
- 設計根拠: `docs/research/*.md` — 技術・API 選定の理由と出典。選定を変える提案をする前に必ず読む

## 不変原則(設計判断で迷ったらこれに従う)

1. **LLM は判断材料を作る側**。発注・サイジング・リスク・会計の経路は決定論的コードのみ。LLM の確信度を直接ポジションサイズにしない
2. **二系統の分離**: 取引=デモ/実、会計=架空(ファンド帳簿)/実費(運営帳簿)。帳簿間の混合はスキーマ制約で禁止。book_id をまたぐ参照を書かない
3. **証憑とリネージ**: 仕訳は evidence_id 必須。データ・生成物には producer_job / code_version / input_refs / as_of を記録
4. **point-in-time**: 分析・バックテストは as_of スナップショットに対してのみ。未来情報の混入はバグではなく設計違反
5. **E1〜E8**(定義は 00-system-design.md §1): LLM 関与戦略の評価はカットオフ後期間のみ/前向き検証/等配分 buy-and-hold 対照/全コスト込み/複数シード/point-in-time ユニバース/単一エージェント対照/資本スケール(¥10万〜100万)&開始時点スイープ
6. **保護領域**(変更に二重確認、監査対象): コンプライアンスゲート・リスクリミット・Kill Switch・会計エンジン・監査部門コード・執筆規格。**IPS はユーザーのみ起票可**
7. **モデル階層**: まず非LLM(ルール・古典ML)で済むか検討 → 軽量 LLM → 中位 → Fable(研究設計・論文執筆・重要レビューのみ。論文ノルマ1日1本)

## 文体・成果物の規約

- 価値のある文章(レポート・論文・設計文書)は執筆規格に従う: 一文アーギュメント先行、パラグラフは抽象度 4→3→2→1→3→5 の U字、レベル1(ファクト)は出典必須
- 報道部の投稿: 毎朝 10:00 JST、最大5トピック、各論文アブストラクト分量、embed にアニメの女の子のマスコット画像。完全非公開サーバーのため個別銘柄推奨は許可
- 用語は 00-system-design.md の用語定義に従う。新しい用語を導入するときは必ず定義を書く

## 将来アクションの制度化(セッション内の約束は無効)

- LLM セッションは使い捨てであり、「あとで私がリマインドする/やる」という発話はセッション終了とともに消える。**将来のアクションは必ず `ops/reminders.yaml` に機械可読で登録すること**
- 登録されたリマインダーの実行主体は、移行期間中は Claude 定例ルーチン(`ryza-weekly-check`)、Ryza 本体の実装後は経営管理部ジョブ(Cloud Scheduler + Discord)
- 実行したリマインダーは status を更新し、履歴を残す

## 作業体制

- 設計・実装指示は Fable(設計リード)が作成、実装は中位モデル(Opus 4.7 / zlm 等)が担当
- 実装指示書は `docs/tasks/` にモジュール単位で置く(自己完結・この会話の文脈なしで実装可能な粒度)
- コスト意識: LLM 呼び出しには部門・タスク種別タグを付ける設計。無駄な高位モデル呼び出しを書かない
